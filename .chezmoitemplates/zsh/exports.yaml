export:
  - name: ZINIT_HOME
    value: {{ joinPath "$HOME" .externals.zinit.dest }}
    resource:
      type: git-repo
      url: "https://github.com/zdharma-continuum/zinit.git"
  - name: ZCOMP_CACHE
    value: {{ joinPath "$HOME" .filesystem.cacheDir "zcomp" }}
    resource:
      type: directory
